def get_password(n):
    pass_ = []
    for i in range(1, 21):
        for j in range(i + 1, 21):
            if n % (i + j) == 0:
                pass_.append(f"{i}{j}")
                result = ''.join(pass_)
                return result


input_number = int(input('Введите число от 3 до 20: '))
print(*get_password(input_number), ' - Вы разгадали пароль')
